{
  "name": "claude-canvas",
  "version": "0.1.0",
  "description": "Claude Code skill that renders interactive diagrams on a real React Flow canvas",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --test 'tests/*.test.mjs'",
    "test:watch": "node --test --watch 'tests/*.test.mjs'",
    "build:app": "bash scripts/build-app.sh",
    "server": "node server.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/notpritam/claude-canvas.git"
  }
}
